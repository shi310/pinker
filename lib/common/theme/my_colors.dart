import 'package:flutter/material.dart';

class MyColors {
  // 主色-固定色
  static const primaryColor = Color.fromARGB(255, 0, 154, 0);

  // 主色-浅-固定色
  static const primaryColorLight = Color.fromARGB(255, 2, 190, 2);

  // 主色-深-固定色
  static const primaryColorDark = Color.fromARGB(255, 0, 154, 0);

  // 白色-固定色
  static const white = Color.fromARGB(255, 255, 255, 255);

  // 黑色-固定色
  static const black = Color.fromARGB(255, 0, 0, 0);

  // 灰色文字
  static const textDisable = Color.fromARGB(255, 181, 182, 181);

  // 次要颜色
  static const secondColor = Color.fromARGB(255, 219, 100, 16);

  // 半透明灰色
  static const geryTranslucent = Color.fromARGB(127, 127, 127, 127);

  // 按钮禁用
  static const filledButtonDisableColor = Color.fromARGB(255, 126, 128, 126);

  // 按钮禁用的文字颜色
  static const onFilledButtonDisableColor = Color.fromARGB(255, 181, 182, 181);
}
